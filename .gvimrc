
" set theme
set background=dark
colorscheme molokai

" set font

" 1.)
" In gvim, you can change the font using the Edit menu, Select Font.
" An alternative is to enter the command:
" :set guifont=*
"
" 2.)
" Once you have a font you like, you want to make it the default in the future. Do ...
" :set guifont?
" ... and Vim will display something like: guifont=Lucida_Console:h11

set guifont=Sauce\ Code\ Powerline\ Regular\ 13

"set linespace=6
set antialias

" no coursor-blink
"set guicursor=a:ver1,a:blinkon0

" no coursor-blink in normal mode
set guicursor=n:blinkon0
